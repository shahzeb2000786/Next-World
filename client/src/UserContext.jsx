import {createContext} from "react";

export const UserContext = createContext(null)//this crates a userContext which will contain the signed in user info and gets exported, and then imported in the app.js
